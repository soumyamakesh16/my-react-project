import LocationPage from "@/react-app/pages/LocationPage";
import { keralaData } from "@/react-app/data/locations";

export default function KeralaPage() {
  return <LocationPage data={keralaData} />;
}
